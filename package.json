{
  "name": "1bitkindle-android",
  "version": "1.0.0",
  "description": "1-bit Kindle drawing tool — Android sideload build",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step — single HTML file'",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/toast": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^6.0.3"
  }
}
